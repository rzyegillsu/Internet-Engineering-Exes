# Teacher Assistant Quiz

یک پلتفرم آزمون گروهی ساده و آموزشی با Next.js که در آن معلم با ویرایش فایل‌های JSON آزمون را مدیریت می‌کند و دانشجویان از طریق رابط وب شرکت می‌کنند.

## 🎯 ویژگی‌ها

- **مدیریت ساده برای معلم**: کنترل کامل از طریق ویرایش فایل‌های JSON
- **رابط کاربری دانشجو**: ورود با نام، پاسخ به سوالات، مشاهده رتبه‌بندی
- **به‌روزرسانی Real-time**: با استفاده از polling (قابل ارتقا به WebSocket)
- **رتبه‌بندی هوشمند**: مرتب‌سازی بر اساس تعداد پاسخ صحیح و زمان پاسخ
- **بدون نیاز به دیتابیس**: ذخیره‌سازی با فایل‌های JSON

## 📁 ساختار پروژه

```
teacher-assistant-quiz/
├── data/
│   ├── questions.json      # سوالات آزمون
│   ├── scores.json         # نمرات دانشجویان
│   └── session.json        # وضعیت جلسه
├── pages/
│   ├── index.js           # صفحه اصلی
│   ├── join.js            # ورود دانشجو
│   ├── quiz.js            # صفحه آزمون
│   ├── scoreboard.js      # رتبه‌بندی
│   ├── _app.js            # تنظیمات App
│   └── api/
│       ├── questions.js   # API دریافت سوالات
│       ├── session.js     # API وضعیت جلسه
│       ├── scores.js      # API نمرات
│       └── answers.js     # API ثبت پاسخ
├── lib/
│   └── fsdb.js           # کتابخانه خواندن/نوشتن فایل‌ها
├── styles/
│   └── globals.css       # استایل‌های سراسری
├── package.json
└── next.config.js
```

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- Node.js 18 یا بالاتر
- npm یا yarn

### مراحل نصب

1. نصب وابستگی‌ها:
```bash
cd teacher-assistant-quiz
npm install
```

2. اجرای سرور توسعه:
```bash
npm run dev
```

3. باز کردن در مرورگر:
```
http://localhost:3000
```

## 👨‍🏫 راهنمای معلم

### مدیریت سوالات

فایل `data/questions.json` را ویرایش کنید:

```json
[
  {
    "id": 1,
    "question": "حاصل 2 + 2 چیست؟",
    "options": ["3", "4", "5", "2"],
    "answer": 1
  }
]
```

- `id`: شناسه منحصر به فرد سوال
- `question`: متن سوال
- `options`: آرایه گزینه‌ها
- `answer`: ایندکس پاسخ صحیح (از 0 شروع می‌شود)

### کنترل جلسه آزمون

فایل `data/session.json` را ویرایش کنید:

```json
{
  "isOpen": true,
  "activeQuestionId": 1
}
```

- `isOpen`: `true` = آزمون باز، `false` = آزمون بسته
- `activeQuestionId`: شناسه سوال فعال فعلی

**مثال**: برای فعال کردن سوال 2:
```json
{
  "isOpen": true,
  "activeQuestionId": 2
}
```

**مثال**: برای بستن آزمون:
```json
{
  "isOpen": false,
  "activeQuestionId": 2
}
```

### مشاهده نتایج

فایل `data/scores.json` حاوی نتایج همه دانشجویان است:

```json
[
  {
    "name": "علی",
    "correct": 2,
    "total": 3,
    "lastAnswerAt": 1734156000000
  }
]
```

## 👨‍🎓 راهنمای دانشجو

### 1. ورود به آزمون

- از صفحه اصلی روی "ورود به آزمون" کلیک کنید
- نام خود را وارد کنید
- وارد صفحه آزمون شوید

### 2. پاسخ دادن به سوالات

- سوال فعال را ببینید
- یکی از گزینه‌ها را انتخاب کنید
- بازخورد فوری (درست/نادرست) دریافت کنید

### 3. مشاهده رتبه‌بندی

- در صفحه آزمون یا از منوی اصلی
- رتبه‌ها هر 3 ثانیه به‌روز می‌شوند

## 🔌 API Documentation

### GET `/api/session`

دریافت وضعیت جلسه

**پاسخ:**
```json
{
  "isOpen": true,
  "activeQuestionId": 1
}
```

### GET `/api/questions`

دریافت همه سوالات

**پاسخ:**
```json
{
  "questions": [
    {
      "id": 1,
      "question": "سوال؟",
      "options": ["گزینه 1", "گزینه 2"],
      "answer": 0
    }
  ]
}
```

### POST `/api/answers`

ثبت پاسخ دانشجو

**درخواست:**
```json
{
  "name": "علی",
  "questionId": 1,
  "optionIndex": 1
}
```

**پاسخ:**
```json
{
  "ok": true,
  "isCorrect": true
}
```

### GET `/api/scores`

دریافت رتبه‌بندی

**پاسخ:**
```json
{
  "scores": [
    {
      "name": "علی",
      "correct": 5,
      "total": 6,
      "lastAnswerAt": 1734156000000
    }
  ]
}
```

## 🎨 سفارشی‌سازی

### تغییر رنگ‌ها

فایل `styles/globals.css` را ویرایش کنید:

```css
body {
  background: linear-gradient(135deg, #e0f7fa 0%, #b2dfdb 100%);
}

h1, h2, h3 {
  color: #11998e;
}
```

### تغییر فاصله polling

در فایل‌های `quiz.js` و `scoreboard.js`:

```javascript
const interval = setInterval(async () => {
  // ...
}, 2500); // میلی‌ثانیه
```

## 🔮 توسعه‌های پیشنهادی

### 1. تایمر سؤال
اضافه کردن `deadline` به `session.json` و نمایش شمارش معکوس

### 2. جلوگیری از پاسخ تکراری
ذخیره `answeredQuestions` برای هر دانشجو

### 3. احراز هویت
اضافه کردن `pin` به `session.json` برای کنترل دسترسی

### 4. WebSocket
جایگزینی polling با Socket.IO برای به‌روزرسانی لحظه‌ای

### 5. پایگاه داده
مهاجرت از فایل‌های JSON به MongoDB یا PostgreSQL

### 6. داشبورد معلم
ساخت رابط گرافیکی برای مدیریت آزمون

## 🛡️ نکات امنیتی

- اعتبارسنجی ورودی‌ها در API انجام شده است
- برای استفاده در تولید، اضافه کردن rate limiting توصیه می‌شود
- برای محیط چند کاربره، استفاده از دیتابیس الزامی است
- قبل از پایان کلاس از `data/scores.json` پشتیبان بگیرید

## 📦 دیپلوی

### Vercel (توصیه شده برای تست)

```bash
npm install -g vercel
vercel
```

⚠️ **توجه**: به دلیل استفاده از filesystem، برای محیط تولید نیاز به هاست Node.js اختصاصی یا مهاجرت به دیتابیس دارید.

### VPS / هاست اختصاصی

```bash
npm run build
npm start
```

## 🧪 تست

### تست سناریو دانشجو

1. چند مرورگر یا حالت ناشناس باز کنید
2. با نام‌های مختلف وارد شوید
3. به سوالات پاسخ دهید
4. رتبه‌بندی را مشاهده کنید

### تست سناریو معلم

1. `data/session.json` را ویرایش کنید → سوال عوض شود
2. `isOpen: false` کنید → پاسخ‌دهی غیرفعال شود
3. `data/questions.json` را ویرایش کنید → سوالات جدید

## 📚 مفاهیم آموزشی

این پروژه شامل:

- ✓ Next.js Pages Router
- ✓ API Routes
- ✓ React Hooks (useState, useEffect)
- ✓ Filesystem operations در Node.js
- ✓ REST API design
- ✓ Client-side polling
- ✓ Local storage
- ✓ کار با JSON

## 🤝 مشارکت

برای بهبود پروژه:

1. Fork کنید
2. تغییرات را اعمال کنید
3. Pull Request ارسال کنید

## 📝 لایسنس

این پروژه برای اهداف آموزشی طراحی شده و آزاد است.

---

**توسعه‌دهنده**: رضیه کرمی  
**شماره دانشجویی**: 40138823  
**مخزن گیت‌هاب**: [Internet-Engineering-Exes](https://github.com/rzyegillsu/Internet-Engineering-Exes.git)
